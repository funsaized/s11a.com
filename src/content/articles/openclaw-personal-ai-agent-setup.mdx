---
title: 'I Gave Myself an AI Agent: My Discord-Powered Automation Stack'
slug: openclaw-personal-ai-agent-setup
excerpt: >-
  For the last few months I've been building something I didn't know I needed: a personal AI agent
  that's always on, connected to my calendar, my smart home, and my daily workflows. Here's the
  architecture, the why, and what it's actually like to have a digital operator running 24/7.
date: '2026-03-31'
category: Automation
tags:
  - AI
  - OpenClaw
  - Home Automation
  - Productivity
  - Discord
readingTime: 12 min read
featured: false
author: Sai Nimmagadda
---

![My Discord channel acting as mission control for a personal AI agent](/images/articles/openclaw-discord-briefing.png)

I didn't set out to build this. I set out to answer a question that had been nagging me: what if the AI tools I was using could actually *do* things, not just answer questions?

I was treating AI like a fancy search engine. Ask a question, get an answer, done. But the real power of LLMs isn't in answering trivia. It's in the gap between "I should check my calendar" and actually having your day organized before you've finished your first coffee.

The result is a personal AI agent running on a Hetzner VPS, connected to Discord, iCloud Calendar, Home Assistant, and a growing suite of automation workflows. It's not perfect. It's not fully autonomous. But it's been running every morning for months now, and I've learned enough to know what works and what doesn't.

## The Architecture

The setup splits the agent brain (gateway) from the execution layer (my Mac Mini) via an encrypted SSH tunnel. The VPS handles AI processing and routing. The Mac Mini handles anything that needs to touch the local network.

![The architecture: VPS gateway connecting Discord, iCloud, and Tavily, tunneling to Mac Mini for Home Assistant and browser control](/images/articles/openclaw-architecture-diagram.png)

**Hetzner VPS (Ubuntu)** — Always-on gateway running OpenClaw. Handles cron scheduling, skill execution, session management, and message routing.

**Discord** — Primary interface. I DM the bot or mention it in a dedicated channel. Both the morning briefing and end-of-day recap post here automatically.

**iCloud Calendar (CalDAV)** — Direct HTTPS calendar access via CalDAV protocol. No middleware. The agent reads and writes events directly to iCloud. Calendar.app on my iPhone and Mac sync natively. Tiimo imports from iCloud.

**Tavily** — AI-optimized web search. Powers research queries in the morning briefing.

**SSH Tunnel** — Mac Mini initiates an outbound connection to the VPS. The VPS forwards its port 18789 to the Mac Mini's port 18790. This means the Mac Mini works behind a residential NAT without requiring the VPS to reach into my home network.

**Mac Mini M1** — Runs the OpenClaw Companion app and a Home Assistant VM (UTM). The Companion app acts as the exec host, running approved binaries on behalf of the gateway.

**Home Assistant (UTM VM)** — Controls Philips Hue (Zigbee hub), Nanoleaf Elements hex panels and Shapes triangles (WiFi), and a Govee Dome floor lamp (WiFi). All under one REST API.

The SSH tunnel architecture is worth unpacking: the Mac Mini connects *out* to the VPS. The VPS doesn't need to punch through my router. If the Mac Mini goes to sleep, the tunnel drops. When the Mac Mini wakes up, autossh reconnects automatically. Simple, but took me a while to get right.

## How the Agent Thinks: SOUL.md

The agent's personality and operational rules live in `SOUL.md` — a file I've written to encode how I want this thing to behave.

SOUL.md defines:

**Core behavior** — "Be genuinely useful, not performative." "Push back clearly when requests are weak, risky, or inconsistent." "Never fake certainty." These aren't soft suggestions. They're the operating constraints that shape every response.

**Communication style** — Direct, concise, data-driven. Short structure for operational updates. Examples and theory for teaching. Candor when blocked by constraints.

**Standards** — Security first with credentials. Ask before destructive actions. Record durable preferences in files.

**Operational reference** — Execution routing, calendar discipline, briefing standards, smart home devices/scenes, model routing, and research protocols all live in a separate `docs/operational-config.md`. I can update operational details without touching the personality layer.

The key discipline in SOUL.md is around **smart home routing**. The agent knows to explicitly announce when it's sending commands through the Mac Mini. That explicitness means I always know where a command is going.

## What the Agent Knows: USER.md and MEMORY.md

USER.md holds the static facts: name, timezone, location, professional context (healthcare tech stack, cloud platforms, databases), fitness goals, interests, calendar setup, and infrastructure overview.

MEMORY.md is synthesized from daily notes. Distilled patterns and preferences that don't live in USER.md because they came from experience rather than initial setup. Things like:

- Writing style preferences (use the humanizer for drafts)
- Tone in direct messages (more informal, jokey)
- Duplicate delivery prevention rules
- Operational lessons
- Email triage patterns
- System health monitoring patterns

The distinction matters: MEMORY.md is **private-chat only**. In group chats, the agent operates without it.

Daily notes live in `memory/YYYY-MM-DD.md`. I don't reference them in group contexts either. Every few days during heartbeats I distill the raw notes into MEMORY.md.

## Skills: What the Agent Can Do

Skills in OpenClaw are plugin packages that extend the agent's capabilities. They're installed via ClawHub onto the gateway.

**Productivity skills:**

- **caldav-calendar** — iCloud CalDAV. List, create, delete events.
- **tavily-search** — AI-optimized web search. Primary tool for weather, market data, research queries.
- **home-assistant** — Smart home control. Exec routed to Mac Mini node via SSH tunnel.
- **people-memories** — Contact memory with SQLite backend.
- **humanizer** — Detects and removes AI writing patterns from text drafts.
- **prompt-agent** — Prompt engineering assistant.
- **self-improving-agent** — Logs learnings, errors, corrections automatically.

**Security skills (ClawSec suite):**

- **clawsec-suite** — Security advisory feed and monitoring.
- **clawsec-feed** — Signed advisory data feed with NVD CVE polling.
- **clawsec-nanoclaw** — Integrity monitoring for skills and workspace.
- **openclaw-audit-watchdog** — Automated security audits with email reports.
- **soul-guardian** — Monitors SOUL.md for unauthorized changes.
- **clawtributor** — Community incident reporting for AI agents.

**Custom workspace skills:**

- **knowledge-base** — Full RAG system. Hybrid search (vector + keyword via Reciprocal Rank Fusion), SQLite + sqlite-vec backend, Gemini embedding-001 at 768 dimensions with local fallback.
- **home-assistant** — Node-exec version of the HA skill, runs on Mac Mini.
- **tavily-search** — Same as the ClawHub skill but workspace-level.
- **clawsec** — Workspace-level ClawSec Monitor operations.

## Scheduled Work: Cron Jobs

The agent runs on a schedule, not just in response to prompts.

**Morning briefing** — 4:00 AM ET, daily. Fetches weather, pulls calendar from iCloud, queries prediction market activity via Tavily, grabs Hacker News top 5 via Firebase API, fetches tech headlines. Composes under 40 lines and posts to Discord.

**Market recap** — 5:00 PM ET weekdays. Market close summary, prediction market highlights, posts to Discord.

**OpenClaw version watch** — 9:00 PM ET, daily. Checks whether a newer OpenClaw version is available.

**Nightly security review** — 2:00 AM ET, daily. Runs full security audit.

**Nightly log ingest** — 7:30 AM ET, daily.

**Health morning brief** — 4:30 AM ET, daily. Parses Oura sleep/readiness data and Apple Health XML export.

**Antfarm polling crons** — Every 5 minutes. Check for pending work and spawn agents.

## Subagent Policy: When to Spawn

The agent doesn't do everything in a single session. Some tasks go to subagents.

**Route to a subagent when:**
- Multi-step tasks with side effects or paid API calls — explain the plan and ask "Proceed?" first
- External API calls (web search, etc.) — so they don't block the main session
- Coding, debugging, investigation — keeps the main session responsive
- Any task that takes more than ~30 seconds

**Run directly when:**
- Simple one-liner fixes
- Reading files or code
- Thread-bound ACP harness requests (Discord thread sessions)
- Anything in the workspace directory

The model routing is centralized in `config/model-routing.json`. Different tasks route to different models.

## The Antfarm Multi-Agent System

For complex multi-step workflows, I use [Antfarm](https://github.com/snarktank/antfarm) — a sub-agent orchestration system that runs multi-agent pipelines where specialized agents work in sequence, each with fresh context. State persists through SQLite.

Installed at `~/.openclaw/workspace/antfarm/`. Workflows are defined in YAML.

The **Competitive Intelligence Scanner** is the flagship workflow — an 8-step pipeline that maps the healthcare AI competitive landscape and produces a strategic briefing.

```
Landscaper → Product Scanner → Regulatory Scanner → Tech Scanner → Research Scanner → Analyst → Trends Analyst → Briefer
```

Each agent gets specific context from the previous step's output. The briefer at the end synthesizes everything into a Discord post.

Other workflows:
- **Topic Mastery Roadmap** (7 agents)
- **Healthcare AI Research** (7 agents)
- **Web Research and Scrape** (7 agents)
- **Wedding Speech Research** (5 agents)

Built-in Antfarm workflows:
- **security-audit** (7 agents)
- **feature-dev** (7 agents)
- **bug-fix** (6 agents)

## Memory That Survives Sessions

Agents in OpenClaw don't carry context between sessions. Files are the only persistence.

**Daily notes** (`memory/YYYY-MM-DD.md`) — Raw session capture. Primary scratch layer. Confidential (private chat only).

**MEMORY.md** — Synthesized from daily notes. Curated patterns and preferences. Only loaded in direct/private chats.

**Heartbeat state** (`memory/heartbeat-state.json`) — Tracks when health checks last ran.

The self-improvement system logs to:
- **LEARNINGS.md** — Correction log
- **ERRORS.md** — Error log

Both are powered by the `self-improving-agent` skill, which hooks into prompt submission and tool use events.

## The Diagnostic Toolkit

I built tools to monitor the tools. This felt necessary once the system grew past a certain complexity.

Four scripts in `scripts/diagnostics/`:

**System health check** — Five-way check. Has exponential backoff on alerts.

**Cron debug tools** — Query, persistent-failures, stale-cleanup.

**Log viewer** — Reads both JSONL streams and SQLite.

**Model diagnostics** — Status, canary, usage.

The canary tests revealed that only MiniMax has an active API key in this environment. Anthropic models use OAuth. OpenAI models hit rate limits. Good to know.

## Smart Home Control

The Home Assistant integration was the part I was most skeptical about, and it's ended up being the most satisfying.

Three lighting ecosystems that don't natively talk to each other: Philips Hue, Nanoleaf, and a Govee Dome. Home Assistant brings them all under one REST API.

I defined scenes in SOUL.md. "Deep work mode" is one command:

- Nanoleaf Elements: reading light effect at 40% brightness
- Nanoleaf Shapes: "Be Productive" effect at 50%
- Govee Dome: white light
- Desk lamp: bright cool white
- Everything in the living room: off

Type "deep work mode" in Discord, watch my office transform in about 2 seconds. No apps, no Bluetooth.

![My desk in deep work mode](/images/articles/openclaw-smart-home-scene.png)

The routing is explicit. The agent knows it's sending commands through the Mac Mini via the SSH tunnel. If the tunnel is down, the command fails cleanly. That fail-fast behavior is what makes this trustworthy.

## The Morning Briefing

The feature that sold me was embarrassingly simple: every morning at 4 AM ET, a cron job fires. The agent wakes up, searches for weather, pulls calendar, queries prediction markets, grabs tech headlines, formats everything into a clean briefing.

```
4:00 AM ET — cron fires
→ Tavily: weather for Morrisville, NC
→ CalDAV: today's calendar events
→ Tavily: prediction market highlights
→ Tavily: top tech/AI headlines
→ Agent composes briefing (< 40 lines)
→ Posts to Discord
```

The impact was immediate and disproportionate to its complexity. Checking all those things manually took maybe 10 minutes — 10 minutes I was either skipping or doing while half-asleep. Now it's done before I'm awake.

Health data landed in the same pipeline a few weeks in. Oura sleep data and Apple Health XML exports were sitting in files I never looked at. Now they're parsed and included. Seeing a trend line without actively asking for it changes the kind of attention you pay to it.

## Security Considerations

Running an always-on agent with calendar access, smart home control, and Discord integration means you've built something with real reach. I didn't treat this lightly.

**Gateway** — Bound to loopback only. Token-based auth. Config file chmod 600.

**Channel access** — Discord guild allowlist, phone number allowlist for WhatsApp, user ID allowlists. Require-mention mode in Discord.

**Exec** — Allowlist-based. Only explicitly approved binaries can run. If the Companion app is unavailable, exec fails.

**Credentials** — Split across encrypted config and systemd environment file. No secrets in shell history.

**Nightly audit** — The 2 AM security review catches configuration drift.

This isn't perfectly hardened. But each layer was a deliberate decision.

## Why This, Not Just... ChatGPT?

I've been asked this a few times. The short answer: automation requires an always-on agent. ChatGPT doesn't run when I'm asleep. It doesn't check my calendar. It doesn't control my lights.

The more interesting answer: the value isn't in the AI's intelligence. It's in the **integration layer**. The AI is the glue between systems that already exist. Calendar, weather, smart home, web search, cron scheduling — none of these are novel. The novelty is having one agent that can reason across all of them, on a schedule, without me initiating anything.

I think about it like the difference between reading about your fitness data in a spreadsheet versus having a coach who shows up every morning and says "here's what happened overnight, here's what you should do today." The data didn't change. The context and timing of delivery changed everything.

## What's Next

The foundation is solid. The daily workflows run reliably. What I want to build next:

- **Health trend analysis** — not just showing daily numbers, but flagging when a metric has been declining over a week
- **Calendar proactive scheduling** — surface conflicts or suggest time blocks based on my Tiimo routine patterns
- **Better browser autonomy** — extend to more complex multi-step browsing workflows

The diagnostic toolkit is documented in the mr-krabs repo alongside the setup guide and architecture docs.

---

If you want to dig into the actual setup, the code lives in the [mr-krabs repository](https://github.com/funsaized/mr-krabs). The architecture doc covers the full topology, the setup guide has the step-by-step, and the diagnostic toolkit is fully documented.

This post is part of me documenting the build in progress. Questions and feedback welcome.